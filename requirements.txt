# ── Dashboard ─────────────────────────────────────────────────────────────────
streamlit>=1.35
plotly>=5.20

# ── Data pipeline ─────────────────────────────────────────────────────────────
pandas>=2.0
numpy>=1.26
requests>=2.31
openpyxl>=3.1          # read USDA RUCC / ICE arrest xlsx

# ── Geospatial (county FIPS crosswalk + spatial join) ─────────────────────────
# geopandas 0.13 is the last Python-3.8-compatible release;
# it requires fiona <1.9 (fiona 1.9+ removed fiona.path used internally)
geopandas>=0.13,<0.14
fiona>=1.8,<1.9
shapely>=1.8,<2.0
